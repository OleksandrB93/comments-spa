# Production Environment Variables for VM
# Set VM_IP to your actual VM IP address

# VM IP Address (change this to your actual VM IP)
VM_IP=172.16.13.248

# Frontend URLs (will use VM_IP automatically)
FRONTEND_URL=http://${VM_IP}:3000
VITE_APP_API_URL=http://${VM_IP}:3001/graphql
VITE_APP_WS_URL=ws://${VM_IP}:3001

# Security settings
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-very-important

# Database credentials
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-this-strong-password
MONGO_DATABASE=comments_db

# Redis password
REDIS_PASSWORD=change-this-redis-password

# RabbitMQ credentials
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=change-this-rabbitmq-password

# Ports
BACKEND_PORT=3001
FRONTEND_PORT=3000
