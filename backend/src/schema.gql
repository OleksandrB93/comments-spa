# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Comment {
  attachment: CommentAttachment
  author: CommentAuthor!
  content: String!
  createdAt: String!
  id: ID!
  parentId: String
  postId: String!
  replies: [Comment!]
}

type CommentAttachment {
  data: String!
  filename: String!
  mimeType: String!
  originalName: String!
  size: Int!
}

input CommentAttachmentInput {
  data: String!
  filename: String!
  mimeType: String!
  originalName: String!
  size: Int!
}

type CommentAuthor {
  email: String!
  homepage: String
  userId: ID!
  username: String!
}

type CommentsResponse {
  allComments: [Comment!]
  comments: [Comment!]!
  limit: Int!
  page: Int!
  totalCount: Int!
  totalPages: Int!
}

input CreateCommentInput {
  attachment: CommentAttachmentInput
  content: String!
  email: String!
  homepage: String
  postId: String!
  userId: String!
  username: String!
}

input CreateReplyInput {
  attachment: CommentAttachmentInput
  content: String!
  email: String!
  homepage: String
  parentId: String!
  postId: String!
  userId: String!
  username: String!
}

type Mutation {
  createComment(input: CreateCommentInput!): Comment!
  createReply(input: CreateReplyInput!): Comment!
  testRabbitMQ: String!
}

type Query {
  commentStats: String!
  comments(postId: String!): [Comment!]!
  commentsPaginated(limit: Int! = 25, page: Int! = 1, postId: String!): CommentsResponse!
  pageViewStats(postId: String!): String!
  popularPosts(limit: Int! = 10): String!
  postCommentCount(postId: String!): Int!
  topCommenters(limit: Int! = 10): String!
  userCommentCount(userId: String!): Int!
  userCommentCountToday(userId: String!): Int!
}