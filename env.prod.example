# Production Environment Variables
# Copy this file to .env and update the values for your VM

# ===========================================
# SECURITY - CHANGE THESE VALUES!
# ===========================================

# JWT Secret - Generate a strong secret key
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-very-important

# Database credentials
MONGO_ROOT_USERNAME=admin
MONGO_ROOT_PASSWORD=change-this-strong-password
MONGO_DATABASE=comments_db

# Redis password
REDIS_PASSWORD=change-this-redis-password

# RabbitMQ credentials
RABBITMQ_USER=admin
RABBITMQ_PASSWORD=change-this-rabbitmq-password

# ===========================================
# APPLICATION SETTINGS
# ===========================================

# Ports
BACKEND_PORT=3001
FRONTEND_PORT=3000

# Frontend URLs (replace with your VM's IP or domain)
FRONTEND_URL=http://your-vm-ip:3000
VITE_APP_API_URL=http://your-vm-ip:3001/graphql
VITE_APP_WS_URL=ws://your-vm-ip:3001

# ===========================================
# OPTIONAL SETTINGS
# ===========================================

# If you want to use a domain name instead of IP
# FRONTEND_URL=https://yourdomain.com
# VITE_APP_API_URL=https://yourdomain.com/api/graphql
# VITE_APP_WS_URL=wss://yourdomain.com/ws

# SSL/TLS settings (if using HTTPS)
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem
